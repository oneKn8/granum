.env
.env.local
__pycache__/
*.pyc
.pytest_cache/
.venv/
node_modules/
.next/
dist/
build/
*.log
.DS_Store
.coverage
htmlcov/
phoenix-audit-data/
synthetic-secrets/

# Demo video working files: rendered voiceover, audit traces, raw footage.
# Local-only; never commit (large, and the scratch voiceover is not the submission).
videos/

# Live germinal-cycle run artifacts (reproducible via `granum cycle`)
runs/

# Local scratch (logs, run backups) — never commit
.scratch/
