.env
.env.local
__pycache__/
*.pyc
.pytest_cache/
.venv/
node_modules/
.next/
dist/
build/
*.log
.DS_Store
.coverage
htmlcov/
phoenix-audit-data/
synthetic-secrets/

# Web-quality audit raw traces (regenerable; multi-MB)
videos/audit/*_trace.json

# Live germinal-cycle run artifacts (reproducible via `granum cycle`)
runs/

# Local scratch (logs, run backups) — never commit
.scratch/
